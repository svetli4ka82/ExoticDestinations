import firebase, { auth } from '../../firebase/firebase';

// export default User = () => {
//     firebase.auth().onAuthStateChanged((currentUser) => {
//         return currentUser;
//     })
// }

